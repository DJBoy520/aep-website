---
// 语言切换占位组件（Step 4 实现真实跳转逻辑）。
// 当前语言用 data-active 属性标记，高亮样式已预留。
// 本步仅做壳：两个按钮并排，点击暂不跳转。
---
<div class="flex items-center gap-1 rounded-full border border-[var(--border-color)] p-0.5 text-sm">
  <button
    type="button"
    class="cursor-pointer rounded-full px-2.5 py-1 text-[var(--text-secondary)] transition-colors hover:text-aep-primary"
    data-active
  >中文</button>
  <button
    type="button"
    class="cursor-pointer rounded-full px-2.5 py-1 text-[var(--text-secondary)] transition-colors hover:text-aep-primary"
  >EN</button>
</div>

<style>
  /* 当前语言高亮（预留，Step 4 切换 data-active） */
  [data-active] {
    background-color: var(--bg-secondary);
    color: var(--text-primary);
    font-weight: 600;
  }
</style>
